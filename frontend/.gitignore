node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native builds (generated by `expo prebuild`) — the managed workflow keeps
# native projects out of source control.
ios/
android/

*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Local environment — never commit real endpoints or secrets.
.env
.env.local
.env.*.local

npm-debug.*
yarn-error.*

.DS_Store
